# 最小WebShell知识包（供深度调查Agent直接使用）


> 文档用途：供深度调查Agent在调查WebShell安全事件时直接引用。
>
> 使用方法：Agent根据当前调查阶段和证据缺口，从以下章节中按需检索对应内容。
>
> 使用约定：
> - Agent在调用本知识包时，调用参数应包含关键词（如 `WebShell攻击原理`、`WebShell处置建议`）。
> - 成功匹配后，返回结果中包含 `evidence_refs`（证据引用），Agent可直接用于填充调查报告。
> - 当前版本面向最小纵向链路，覆盖WebShell场景的核心调查需求。



## 1. Agent 输入与输出约定

### 1.1 Agent 输入格式（上游传入）

Agent 接收上游风险研判后的标准化安全事件，例如：

```json
{
  "event_id": "EVENT-001",
  "event_type": "WebShell",
  "severity": "HIGH",
  "timestamp": "2026-08-22 10:23:15",
  "source_ip": "10.10.10.25",
  "target_ip": "192.168.1.100",
  "initial_verdict": "疑似真实攻击",
  "confidence": 0.72,
  "evidence": [
    "检测到疑似WebShell通信",
    "攻击源与目标存在通信关系"
  ]
}
```

### 1.2 Agent 输出格式（调查报告）

Agent 输出结构化调查结果，包含以下字段

```json
事件基本信息
调查结论
风险等级
攻击类型
关键证据
证据来源
调查步骤
工具调用记录
攻击链/攻击过程
调查置信度
处置建议
是否需要人工接管
```



## 2. 攻击概述

### 2.1 攻击名称

- **WebShell（网页后门）** —— MITRE ATT&CK 技术编号：T1505.003
- 相关术语：一句话木马（如 `<?php @eval($_POST['cmd']); ?>`）、大马（功能完整型）、小马（功能单一型）、内存马（无文件落地型）

### 2.2 攻击原理（Agent回答“这是什么攻击”时使用）

WebShell 是被植入 Web 服务器的恶意脚本。攻击者通过 HTTP/HTTPS 协议与其通信，获得与 Web 服务器进程同权限的远程命令执行能力。

核心特征：

- 通信流量混入正常 Web 流量（80/443 端口），能够穿透防火墙；
- 不在系统日志中留下登录记录，仅可能在 Web 访问日志中留下少量痕迹；
- WebShell 常利用文件上传漏洞、远程代码执行漏洞或服务器配置错误实现植入。

[引用来源：MITRE ATT&CK T1505.003]



## 3. Agent 可检索的知识条目

### 3.1 攻击特征速查表

Agent在调查过程中可通过 `knowledge.query` 工具传入关键词（如 `WebShell攻击特征`）进行检索，返回以下参考信息：

| 攻击类型            | 关键特征                                             | 调查方向              |
| :------------------ | :--------------------------------------------------- | :-------------------- |
| **WebShell 上传**   | 双扩展名、上传路径含 `/uploads/`、文件内容含 `eval(` | 检查文件系统、Web日志 |
| **WebShell 通信**   | POST 请求参数 `z0`/`z1`/`z2`、UA固定、Base64编码     | 检查Web日志、网络连接 |
| **WebShell 持久化** | crontab新增、SSH公钥新增、注册表Run项新增            | 检查计划任务、启动项  |

[引用来源：MITRE ATT&CK T1505.003]
[引用来源：NSA/CISA 联合报告]

### 3.2 主流管理工具与流量特征

Agent在调查过程中可通过 `knowledge.query` 工具传入工具名称（如 `中国菜刀 流量特征`）进行检索，返回以下参考信息：

| 工具                     | 流量特征                                                     | 识别要点                                          |
| ------------------------ | ------------------------------------------------------------ | ------------------------------------------------- |
| **中国菜刀（Chopper）**  | POST 请求参数固定为 `z0`、`z1`、`z2`；数据经 Base64 编码；User‑Agent 固定为 `Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)` | 监控 POST 请求中的固定参数名和固定 UA 字段        |
| **蚁剑（AntSword）**     | User‑Agent 含 `AntSword` 关键字；payload 经过混淆/编码；请求路径特征如 `/api/command`、`/shell` | 监控 UA 中的 AntSword 关键字及路径特征            |
| **冰蝎（Behinder）**     | AES 动态加密通信；Content‑Type 为 `application/octet-stream`；支持 WebSocket 长连接 | 监控二进制加密流和 WebSocket 长连接特征           |
| **哥斯拉（Godzilla）**   | AES/RSA 强加密；随机生成参数名（如 `x1`、`y2`）；支持分块传输（Transfer‑Encoding: chunked） | 监控分块传输和随机参数名特征                      |
| **C99 Shell**            | 常包含 `c99`、`cmd`、`php` 等关键字；界面特征明显；常通过 `?cmd=` 参数传递命令；User-Agent 多为非标准浏览器标识 | 监控 URL 中的 `c99` 或 `cmd` 关键字及非标准 UA    |
| **Weevely**              | HTTP 请求特征包含 `Weevely` 或 `wv` 字段；常通过 `?` 传递加密参数；User-Agent 多为 `Weevely` 或随机标识；支持自定义混淆及模块扩展 | 监控 URL 中的 `Weevely`、`wv` 字段及固定 UA 特征  |
| **隐蔽通道（DNS/ICMP）** | 使用 DNS 隧道或 ICMP 协议进行数据外传；DNS 请求中带有长编码子域名；ICMP 数据包 payload 部分携带数据 | 监控 DNS 请求中的长编码子域名及 ICMP payload 异常 |

|      | 资料来源                                                     |
| ---- | ------------------------------------------------------------ |
| 1    | MITRE ATT&CK, Software S0020 - China Chopper, https://attack.mitre.org/software/S0020/ |
| 2    | CSDN，Webshell管理工具的流量特征，https://blog.csdn.net/w2361734601/article/details/147621839 |

### 

## 4. Agent 证据检查清单

Agent 在调查 WebShell 事件时，应依次检查以下证据项，并在调查报告中记录检查结果：

| 序号 | 检查项                                                 | 证据来源     | 预期发现                             |
| :--- | :----------------------------------------------------- | :----------- | :----------------------------------- |
| 1    | 目标 Web 服务器的 Web 目录下是否存在新增的可疑脚本文件 | 文件系统检查 | 如 `shell.php`、`cmd.jsp` 等异常文件 |
| 2    | 该文件是否具有双扩展名或与业务无关的命名               | 文件系统检查 | 如 `image.jpg.php`、`test.aspx`      |
| 3    | Web 访问日志中是否存在异常 POST 请求或固定参数         | Web 日志分析 | 如 `z0=`、`z1=` 等菜刀特征           |
| 4    | Web 服务器进程是否启动了命令行程序                     | 进程监控     | 如 `w3wp.exe` 启动 `cmd.exe`         |
| 5    | 服务器是否存在异常的对外网络连接                       | 网络监控     | 如非业务时段的外连                   |

[引用来源：NSA/CISA 联合报告]
[引用来源：CISA Eliminate Web Shells (CM0106)]



## 5. Agent 处置建议模板

Agent 输出调查报告时，如需生成处置建议，应参照以下流程引用：

参照 **CISA《Eliminate Web Shells》(CM0106)** 消除指南，建议按以下顺序处置：

1. **隔离（Isolate）**：将受感染的 Web 服务器隔离，减少横向移动的可能性；
2. **保全证据（Preserve Artifacts）**：保留磁盘/内存镜像，以及操作系统、Web 应用、访问、错误、WAF、反向代理、CDN、DMZ 防火墙和数据库等各类日志；
3. **删除 WebShell（Delete the web shell）**；
4. **修改密码**：修改 Web 管理员、数据库用户、托管账户和远程访问（FTP、SSH 等）的密码；
5. **调查根本原因**：调查导致入侵的底层漏洞；
6. **从干净备份恢复（Restore from clean backup）**；
7. **修复和加固**：通过打补丁和加固 Web 应用及 Web 服务器进行修复。

[引用来源：CISA Eliminate Web Shells (CM0106) - "Eliminating Web Shells" 章节]



## 6. Agent 停止条件与人工接管规则

### 6.1 停止条件

满足以下任意条件即停止调查：

1. **证据足够**：已获得支持明确调查结论的关键证据；
2. **达到最大步骤**：调查步骤达到预设上限（MVP阶段建议 `step >= 3）；
3. **工具无法获得数据**：XDR 查询失败、数据为空、权限不足。

### 6.2 人工接管条件

以下情况触发人工接管：

- 高风险事件 + 关键证据不足 + 关键工具调用失败；
- 调查结果存在明显冲突；
- 涉及高风险处置动作且缺少审批。

Agent 在上述情况下应输出“**证据不足，需要人工接管**”，不得自行编造结果。



## 7. 知识包更新记录

| 日期       | 版本 | 更新内容                                                     | 更新人 |
| :--------- | :--- | :----------------------------------------------------------- | :----- |
| 2026-08-23 | v1.3 | 按最小纵向链路交付要求，从完整知识包中抽取Agent核心使用部分，建立检索式知识条目结构 | 沈洪旭 |