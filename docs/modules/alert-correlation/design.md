# 告警关联模块设计

## 模块职责

将一组标准告警关联为安全事件，输出关联依据、时间范围、涉及实体和关联前后数量变化。

## 输入输出

- 输入：`AlertRecord` 列表。
- 输出：`SecurityEvent`。

## 安全边界

- 不直接读取原始平台凭据。
- 不直接修改 `EventContext.status`。
- 不直接调用外部平台接口。

## 待补充

- 关联规则。
- 去重策略。
- 固定样例基线。
- 异常处理。

