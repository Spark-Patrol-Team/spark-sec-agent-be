# 状态编排模块设计

## 模块职责

统一读取阶段结果、写入 `EventContext`、推进业务状态，并控制异常、审批和人工接管路径。

## 输入输出

- 输入：启动请求、审批结果、各业务模块结果。
- 输出：更新后的 `EventContext`。

## 安全边界

- 状态只能由编排层推进。
- 业务模块只返回结果，不直接改状态。
- 非法状态跳转由状态机拒绝。

## 待补充

- 异常恢复策略。
- 重试策略。
- 长流程断点恢复方案。

