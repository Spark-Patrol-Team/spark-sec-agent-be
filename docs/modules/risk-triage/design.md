# 风险研判模块设计

## 模块职责

根据安全事件和辅助信息输出真实性判断、置信度、风险分、优先级、证据引用和是否进入深度调查。

## 输入输出

- 输入：`SecurityEvent`、`AlertRecord` 列表。
- 输出：`TriageResult`。

## 安全边界

- 不只依赖一次大模型问答。
- 不直接执行处置动作。
- 不直接修改业务状态。

## 待补充

- 评分规则。
- 误报判断依据。
- 证据缺口定义。

