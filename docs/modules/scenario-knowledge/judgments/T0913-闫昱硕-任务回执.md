# T0913 闫昱硕：判据与结果 Review（3.7 九项）— 任务回执

> 日期：2026-09-13　|　提交分支：`feature/t0913-yanyushuo-judgment-review-clean`（基于 `origin/main@9247624` 干净重建）　|　协作：杨景凡（`T0905-07` 正式 A/B 结果包）、陈敏（门禁 PR#43）、沈洪旭（`WSK-*` 知识卡）、李雨妍（汇总 Schema）、肖迎春（`T0905-05` 处置边界复核）

## 一、本轮工作

1. **拉取远端更新**：同步 GitHub，`main` 已合入 PR#50（知识门禁 fail-open 修复），head `9247624`。
2. **建干净 PR 分支**：从最新 `main` 重建 `feature/t0913-yanyushuo-judgment-review-clean`，只包含判据、指标与 Review 材料（`judgments/` + 判据守护测试），不含陈敏门禁、知识资产、case 输入等上游代码。
3. **判据绑定最终 case1-10 与 `WSK-*`**：10 份 `caseN.expected.json` 新增 `knowledge_ids_wsk`，并新增守护测试 `test_wsk_binding_matches_final_knowledge_cards`。
4. **删除旧结论**：删除此前在未取得 actual 包时形成的「正式 A/B 已完成」结论（`一致性review.md` 第 5.5 节），改为迁移说明。
5. **接入正式 A/B 结果包**：杨景凡 `T0905-07` 正式 20 份报告已取得；脱敏 `_summary.json` 入仓（`ab-results/T0905-07-OFF-GUARDED-AB-summary.json`），原始 `report_*.json` 含内网测试 IP、按作者说明不入仓。
6. **完成 10 案正式 A/B Review**：10 案矩阵、case6 逐字越界判定、20 案 `need_manual_takeover` 解释、不通过项与最小修改要求，见 `T0913-闫昱硕-正式ABReview与不通过项.md`。

## 二、正式 A/B Review 结论

- **三档门禁与知识放行一致**：`in_scope`（case9）命中 `WSK-010/001/015`、报告 7 条知识引用；`weak_signal`（case1-5、7、8）知识仅 `partial`、报告 0 条确认性引用；`out_of_scope`（case6、10）被 `knowledge_scope_mismatch` 拒绝；`off` 全部 `not_registered`。
- **知识门禁未观察到越界**；但 **case6 报告层越界**：`attack_chain` 写“直接植入后门/WebShell + 创建隐藏管理员实现持久化”，`disposal_suggestions` 写“排查/清除 WebShell”——来源是漏洞情报 `CVE-2024-52377（可致WebShell）` 被 LLM 扩写为本事件攻击事实，属报告生成层约束缺失（非门禁失效）。
- **20/20 `need_manual_takeover=true`**：由「门禁不管自动处置 + 高风险处置需审批 + 域外/工具失败转人工」叠加，属预期保守行为；但该字段无区分度，不能作为分级正确证据，登记为不通过项。
- **处置链 NOT VERIFIED**：报告缺 `response_evidence_scope` / `ResponsePlan` / `disposition`。

## 三、交付物与提交状态

| 文件/分支 | 说明 |
|---|---|
| `feature/t0913-yanyushuo-judgment-review-clean` | 干净提交，基于 `main@9247624`，commit `00c30b1` |
| `judgments/case1.expected.json` ~ `case10.expected.json` | 10 份判据，绑定最终 case1-10 与 `WSK-*` |
| `judgments/T0913-闫昱硕-正式ABReview与不通过项.md` | 交付物 #6 正式 Review + case6 判定 + 不通过项 |
| `judgments/ab-results/T0905-07-OFF-GUARDED-AB-summary.json` | 脱敏 20 行 A/B 汇总（原始报告不入仓） |
| `judgments/一致性review.md` 等 | 交付物 #1-#5：三档规则、来源—主张边界、指标口径、一致性 Review |
| `tests/test_yanyushuo_expected_judgments.py` | 判据守护测试（含 WSK 绑定校验） |

## 四、验证

- `tests/test_yanyushuo_expected_judgments.py`：9 通过（含 WSK 绑定）。
- 判据/知识相关：36 通过。
- 全量 `pytest`：318 通过 / 1 跳过。

## 五、残留 / 待办

- case6 报告层越界：需杨景凡补报告生成约束与自动检查（最小修改要求已给出）。
- case6/case10 判据 `human_required=false` 与报告 `need_manual_takeover=true` 口径冲突：待与杨景凡/李雨妍对齐后定。
- 处置链字段缺失（`response_evidence_scope` / `ResponsePlan` / `disposition`）：待肖迎春/李雨妍/杨景凡明确归属。
- 原始 `report_*.json` 不入仓，case6 逐字判定基于本人本地核对；如需复现需杨景凡提供脱敏版或授权存内部位置。
