# <模块名称>测试记录

> 使用前删除本提示：测试结论必须来自真实执行；不能只写“测试通过”。命令、实际结果、基线Commit和已知限制为必填项。

## 0. 复验信息

| 项目 | 内容 |
|---|---|
| 模块 | `Scenario-Knowledge` |
| 任务/测试批次 | `T0826-08` |
| 执行人 | `沈洪旭` |
| 执行时间 | `2026-08-26 14:37 ` |
| 基线分支与Commit | `T0823-knowledge-mini` |
| 环境 | `GitHub网页端预览 + Markdown阅读器（Typora）` |
| 数据集/样例版本 | `不适用（知识材料为静态文档，无数据集依赖）` |
| 工作流/知识库版本 | `不适用（尚未接入正式知识库）` |
| 能力性质 | `自研代码 （知识材料）` |
| 验收层级 | `模块 + Agent联调` |
| 总体结论 | `阶段通过 ` |
| 关联正式交付章节 | `` |

## 1. 测试范围与不在范围内事项

### 1.1 本轮覆盖

- 知识包和问答样本的Markdown格式检查；
- 引用来源链接有效性验证（5个来源全部可访问）；
- 问答样本与知识包内容一致性核对（5题全部对应）；
- 与深度调查Agent（杨景凡）联调，验证Agent是能够基于知识包得出正确结论；

### 1.2 本轮未覆盖

- 问答样例测试未实现，因为agent不好测问答型的样例，因为agent的输入是一个json文件，主要包含事件ID，事件类型这些东西，而不是一句话；
- 真实XDR数据场景下的知识包使用效果；

## 2. 前置条件与测试数据

- 前置条件：`知识包和样例已上传`
- 测试数据性质：` 人工构造+固定样例 `
- 测试数据位置：`docs/modules/scenario-knowledge/`

## 3. 真实执行命令

```text
# 格式检查（在GitHub网页端预览）
打开 https://github.com/Spark-Patrol-Team/spark-sec-agent-be/pull/8
→ Files changed 标签
→ 预览 webshell-knowledge.md 和 webshell-qa-samples.md
→ 检查所有标题、表格、引用、代码块格式

# 来源链接验证（逐一点击）
MITRE ATT&CK T1505.003: https://attack.mitre.org/techniques/T1505/003/
MITRE S0020: https://attack.mitre.org/software/S0020/
NSA/CISA AA22-138A: https://www.cisa.gov/news-events/cybersecurity-advisories/aa22-138a
CISA CM0106: https://www.cisa.gov/eviction-strategies-tool/info-countermeasures/CM0106
CSDN: https://blog.csdn.net/w2361734601/article/details/147621839

# 问答一致性核对（人工对照）
逐题检查问答样本的预期答案是否可在 webshell-knowledge.md 中找到对应依据
```

## 4. 测试用例与实际结果

| 用例ID | 优先级 | 类型 | 场景/输入 | 预期结果 | 实际结果 | 状态 | `trace_id` | 证据编号 | 缺陷编号 |
|---|---|---|---|---|---|---|---|---|---|
| `001` | `P0` | `联调` | `Agent输入WebShell事件→ 查询“WebShell攻击原理` | `返回攻击原理章节内容` | `正确命中：攻击原理` | `Pass` | `160036` | `<EVID-001>` | `无` |
| `002` | `P0` | `联调` | `Agent输入WebShell事件（蚁剑场景）→ 查询“蚁剑流量特征”` | `返回蚁剑流量特征条目` | `正确命中：主流管理工具和流量特征` | `Pass` | `160036` | `<EVID-002>` | `无` |
| `003` | `P0` | `联调` | `Agent输入WebShell事件（冰蝎场景）→ 查询“冰蝎流量特征”` | `返回冰蝎流量特征条目` | `正确命中：主流管理工具和流量特征` | `Pass` | `160036` | `003>` | `无` |

正式用例状态统一使用`Not Run`、`Pass`、`Fail`、`Blocked`和`Not Applicable`。测试框架自身的`skipped`数量可在结果摘要中另行记录，但不能替代正式用例状态和原因。

## 5. 结果汇总

| 指标 | 数量 |
|---|---:|
| 通过 | `3` |
| 失败 |  |
| 阻塞 |  |
| 未执行 |  |
| 不适用 |  |
| 测试框架skipped（如有） |  |

- 关键状态时间线或输出摘要：一次性通过
- 实际调用的Agent、工具或fallback：深度调查Agent（杨景凡）直接读取Markdown内容
- 与预期不一致项：问答样例无法测试

## 6. 指标贡献与原始计数

只填写本模块能够用真实数据支持的指标；不能可靠计算时写“不具备计算条件”和原因，不填推测值。

| 指标 | 计算口径 | 分子/原始计数 | 分母/原始计数 | 结果 | 数据或脚本证据 |
|---|---|---:|---:|---:|---|
| Agent知识命中准确率 | 正确命中数/总查询数 |             3 |             3 | 100% | EVID-001~003   |

## 7. 证据索引

| 证据 | 位置 | 脱敏状态 | 支持的结论 |
|---|---|---|---|
| `EVID-001~003` | `<GitHub相对路径或链接>` | `已脱敏` | `Agent已可基于知识包回复并写入调查报告` |

## 8. 失败项与已知限制

| 问题 | 复现方式 | 影响 | 当前处理/下一步 |
|---|---|---|---|
| 问答样例不能测试 | `不阻塞` | 暂无 | 因为调查agent还没用真实数据测试，Mcp工具还不知道可不可行；待更多测试之后再补充知识包 |

## 9. 验收结论

- 本轮可确认：知识包和问答样本格式正确，可在GitHub上正常预览；5个来源链接全部可访问，来源可靠；5个问答样本均可在知识包中找到对应依据，一致性100%；深度调查Agent已可调用知识包内容进行回复（WebShell攻击原理、冰蝎流量特征、蚁剑流量特征均已正确命中。
- 本轮不能确认：真实XDR数据场景下的知识包使用效果。
- 是否影响上下游或主链：`否`
- 建议状态：`已验收 `

## 10. 变更记录

| 日期 | 基线Commit | 新增或变更测试 | 结论 |
|---|---|---|---|
| 2026-08-26 |            | 与杨景凡联调验证3组知识查询 | 全部正确命中 |
