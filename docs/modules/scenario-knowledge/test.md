# 场景知识模块测试说明和记录

## 测试范围
## 0. 复验信息

- 知识引用是否可追溯。
- 知识增强前后调查完整性。
- 幻觉和错误引用检查。
| 项目 | 内容 |
|---|---|
| 模块 | `Scenario-Knowledge` |
| 任务或测试批次 | `T0826-08` |
| 材料负责人 | 沈洪旭 |
| 原记录时间 | 2026-08-26 14:37 |
| 原记录基线 | `T0823-knowledge-mini@541a1762` |
| 文档清理时间 | 2026-08-28 |
| 环境 | GitHub Markdown 预览、本地 Git 静态检查 |
| 能力性质 | 文档型知识资产 |
| 验收层级 | 文档静态检查；功能链路待复验 |
| 总体结论 | 文档材料可进入评审；不能据此判定 Agent 联调通过 |

## 待补充
## 1. 测试范围与不在范围内事项

- 固定样例对比。
- 权威资料列表。
- 验收指标。
### 1.1 本记录可确认的范围

- 5 份 Markdown 文件的结构和渲染格式。
- 模板标题、占位字段、重复章节和残缺表格等文档质量问题。
- 知识材料与问答样本已提交到约定目录。

### 1.2 本记录不能确认的范围

- 来源链接当前是否全部可访问、来源是否足够权威，以及每项知识主张是否被来源直接支持。
- Agent 是否通过真实 JSON 事件入口自动调用并命中知识材料。
- 5 个问答样本的真实运行结果。
- 真实 XDR、FastGPT、MCP 或正式知识库平台的接入效果。

## 2. 前置条件与测试数据

- 文档位置：`docs/modules/scenario-knowledge/`。
- 测试数据：本目录内的固定 Markdown 材料和 5 个问答样本。
- 历史反馈：成员曾报告 3 组关键词能够命中对应条目，但原记录未附可追溯命令、输入、日志或截图，因此本次不将其计为独立复验通过。

## 3. 实际执行命令

```powershell
git diff --check
rg -n "[<]模块名称[>]|[<]知识包[>]|[<]EVID-|[<]GitHub相对路径|YYYY[-]MM[-]DD|使用前删除本提[示]" docs/modules/scenario-knowledge
```

判定规则：

- `git diff --check` 无输出且退出码为 0，表示未发现新增空白错误。
- 占位符扫描无结果，表示已清理列出的模板残留。
- 上述静态检查不验证来源或 Agent 功能。

## 4. 测试用例与证据状态

| 用例 ID | 优先级 | 类型 | 场景 | 预期结果 | 当前结果 | 状态 | 证据 |
|---|---|---|---|---|---|---|---|
| `DOC-001` | P0 | 静态检查 | Markdown 格式和空白检查 | 无格式错误 | 退出码 0，无错误输出 | Pass | `git diff --check` |
| `DOC-002` | P0 | 静态检查 | 模板占位符扫描 | 无指定模板残留 | 无匹配结果 | Pass | `rg` 扫描结果 |
| `SRC-001` | P0 | 人工核验 | 逐条检查来源和知识主张 | 每项可追溯且被来源直接支持 | 未执行 | Not Run | 待补来源核验记录 |
| `QA-001` | P1 | 内容核验 | 5 个问答样本与知识材料映射 | 每题有对应依据 | 缺少独立核验记录 | Not Run | 待补逐题核验表 |
| `AGENT-001` | P0 | 功能联调 | 真实 JSON 事件触发知识查询 | 查询词、命中内容和返回状态可追溯 | 未执行 | Not Run | 待补终端日志或截图 |
| `AGENT-002` | P0 | 功能联调 | Agent 报告引用知识内容 | 报告包含可追溯引用 | 未执行 | Not Run | 待补事件 ID、报告和日志 |
| `PLATFORM-001` | P1 | 平台联调 | 真实 XDR / 知识库平台接入 | 真实平台查询成功且边界明确 | 未执行 | Not Run | 待平台环境与权限 |

正式用例状态统一使用 `Not Run`、`Pass`、`Fail`、`Blocked` 和 `Not Applicable`。

## 5. 结果汇总

本表应在实际执行后更新；未附证据的成员反馈不计入通过数。

| 指标 | 当前数量 |
|---|---:|
| Pass | 2 |
| Fail | 0 |
| Blocked | 0 |
| Not Run | 5 |
| Not Applicable | 0 |

## 6. 指标口径

当前没有足够证据计算“Agent 知识命中准确率”。只有在保存每次输入、查询词、实际命中、预期结果和运行日志后，才可按“正确命中数 / 总查询数”计算该指标。

原记录中的“3/3、100%”仅来自成员描述，缺少对应证据文件，本次不沿用为正式验收指标。

## 7. 证据索引

| 证据 | 位置 | 支持的结论 |
|---|---|---|
| `EVID-DOC-001` | 本地提交前的 `git diff --check` 输出 | 是否存在新增空白错误 |
| `EVID-DOC-002` | 本地提交前的模板占位符扫描输出 | 是否仍有指定模板残留 |
| PR 变更记录 | [PR #8](https://github.com/Spark-Patrol-Team/spark-sec-agent-be/pull/8) | 文档材料和修改历史 |
| 功能联调证据 | 尚未提供 | 暂不支持 Agent 已联调通过的结论 |

## 8. 已知限制与下一步

| 问题 | 影响 | 下一步 |
|---|---|---|
| 来源口径尚未逐条复核 | 不能确认知识内容正式验收 | 负责人补充来源核验记录 |
| 3 组关键词查询缺少日志或截图 | 不能计为独立复验通过 | 在目标主干重新执行并保存证据 |
| 问答样本与 Agent JSON 入口不一致 | 5 个样本无法直接证明主链能力 | 建立等价事件用例或提供专用评测入口 |
| 未确认目标主干实际消费哪套知识文件 | 可能出现双份材料或测试错基线 | 运行时负责人确认代码路径和 Commit |
| 真实平台尚未接入 | 不能评价真实数据下的检索效果 | 平台就绪后执行端到端验证 |

## 9. 验收结论

- 当前可确认：知识材料和问答样本已提交；文档质量问题正在本次 PR 中清理。
- 当前不能确认：来源全部可靠、5 个问答样本通过、Agent 已自动调用知识材料、真实平台已接入。
- 建议状态：`文档待评审，功能待复验`。
- 是否影响主链：不阻塞文档评审；若主链依赖该知识材料自动检索，则在功能复验完成前仍存在验收缺口。

## 10. 变更记录

| 日期 | 基线 | 变更 | 结论 |
|---|---|---|---|
| 2026-08-26 | `T0823-knowledge-mini@541a1762` | 原记录声称 3 组关键词命中 | 缺少可追溯证据，不能独立复现 |
| 2026-08-28 | PR #8 文档清理提交 | 清理模板残留并收紧测试结论 | 2 项静态检查通过；功能待复验 |
