# 前端模块设计

## 模块职责

展示原始告警、关联事件、风险研判、调查步骤、工具调用、处置建议、审批状态、执行状态和验证结果。

## 输入输出

- 输入：后端 HTTP API 返回的事件列表、事件详情和时间线。
- 输出：前端页面和用户审批操作。

## 安全边界

- 不展示明文凭据。
- 不展示未经整理的模型内部自由文本推理。
- 高风险动作必须通过审批入口触发。

## 待补充

- 页面结构。
- 字段映射。
- 交互流程。

