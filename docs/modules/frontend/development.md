# 前端模块开发说明

## 代码位置

当前仓库为后端项目，前端代码位置待确认。

## 接入方式

优先对接后端最小接口：

- `GET /health`
- `POST /runs`
- `GET /events`
- `GET /events/{event_id}`
- `GET /events/{event_id}/timeline`
- `POST /events/{event_id}/approval`
- `GET /metrics`

## 待补充

- 前端仓库或目录。
- 联调环境。
- 页面字段需求。

